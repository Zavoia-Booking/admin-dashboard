# Loaded by Vite when running `yarn dev --mode test` (used by Playwright's webServer).
# Points the dev server's /api proxy at the test admin-api on port 3001.
VITE_API_PROXY_TARGET=http://localhost:3001
